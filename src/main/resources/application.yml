spring:
  application:
    name: MesaFacil

  datasource:
    url: jdbc:postgresql://localhost:5431/MesaFacil
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true

    flyway:
      enable: true
      locations: classpath:db/migration